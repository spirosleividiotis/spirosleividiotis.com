# Locked production site

This is the **locked production version** of spirosleividiotis.com.

- **Hero name:** left-aligned at all screen sizes.
- **About me (mobile):** full bio visible, no clipping; block layout, no parallax on about panel.
- **Desktop:** unchanged; about overlay as designed.
- **Content:** driven by `content.json`. Edit content via the CMS only.

Do not change layout/CSS/JS for the live site without explicit need. All content changes go through the CMS.
